name: test
channels:
  - openeye
dependencies:
    # Base depends
  - python
  - pip
  - openeye-toolkits
  - pyyaml
  - numpy
  - matplotlib
  - scipy

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
