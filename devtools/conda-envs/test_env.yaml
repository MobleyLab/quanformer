name: test
channels:
  - openeye
dependencies:
    # Base depends
  - python
  - pip
  - openeye-toolkits
  - pyyaml

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
