name: test
channels:
  - defaults
  - openeye
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - openeye-toolkits
  - pyyaml
  - numpy
  - matplotlib
  - scipy

    # QCFractal testing
  - qcfractal
  - rdkit
  - geometric

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
